{
  "name": "firechain",
  "version": "1.0.0",
  "description": "framework for blockchain apps",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir lib"
  },
  "author": "",
  "dependencies": {
    "web3": "^1.3.1"
  },
  "babel": {
    "presets": ["es2015"]
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1"
  }
}
